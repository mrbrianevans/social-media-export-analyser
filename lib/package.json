{
  "name": "social-media-export-analyser-lib",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "docs": "npx typedoc --entryPointStrategy expand ./ --out ../docs"
  },
  "dependencies": {
    "camelcase": "^6.2.0",
    "cheerio": "^1.0.0-rc.10",
    "snake-case": "^3.0.4",
    "ua-parser-js": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.12",
    "@types/ua-parser-js": "^0.7.36"
  }
}
